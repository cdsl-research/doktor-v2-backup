apiVersion: batch/v1
kind: CronJob
metadata:
  name: paper-minio-backup-job
  namespace: backup
spec:
  schedule: "00 15 * * *" # JST 00:00
  jobTemplate:
    spec:
      template:
        spec:
          nodeSelector:
            node-role.kubernetes.io/master: "true"
          imagePullSecrets:
          - name: regcred
          containers:
          - image: ghcr.io/cdsl-research//paper-minio-backup:1.0
            name: paper-minio-backup
            imagePullPolicy: Always
            volumeMounts:
            - name: paper-minio-backup-volume
              mountPath: /backup
          restartPolicy: Never
          volumes:
          - name: paper-minio-backup-volume
            hostPath:
              path: /home/cdsl/backup/paper-minio
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1