apiVersion: batch/v1
kind: CronJob
metadata:
  name: author-mongo-backup-job
  namespace: backup
spec:
  schedule: "00 15 * * *" # JST 00:00
  jobTemplate:
    spec:
      template:
        spec:
          # imagePullSecrets:
          # - name: regcred
          containers:
          - name: author-mongo-backup
            image: c0a22100/doktor-mongo-backup:latest
            # image: ghcr.io/cdsl-research/doktor-mongo-backup:1.0
            imagePullPolicy: Always
            envFrom:
            - configMapRef:
                name: backup-config
            env:
            - name: MONGO_PORT
              value: "30300"
            - name: MONGO_DB
              value: "author"
            - name: MONGO_COLLECTION
              value: "author"
            - name: MONGO_OUTPUT
              value: "author"
            volumeMounts:
            - name: nfs-volume
              mountPath: /backup
          volumes:
          - name: nfs-volume
            nfs:
              server: 192.168.100.3
              path: /volume1/doktor-dev-backup/
              # readOnly: false
          restartPolicy: OnFailure
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1